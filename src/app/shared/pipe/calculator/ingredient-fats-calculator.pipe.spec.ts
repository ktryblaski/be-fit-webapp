import { IngredientFatsCalculatorPipe } from './ingredient-fats-calculator.pipe';

describe('IngredientFatsCalculatorPipe', () => {
  it('create an instance', () => {
    const pipe = new IngredientFatsCalculatorPipe();
    expect(pipe).toBeTruthy();
  });
});

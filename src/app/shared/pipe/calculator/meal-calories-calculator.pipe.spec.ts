import { MealCaloriesCalculatorPipe } from './meal-calories-calculator.pipe';

describe('MealCaloriesCalculatorPipe', () => {
  it('create an instance', () => {
    const pipe = new MealCaloriesCalculatorPipe();
    expect(pipe).toBeTruthy();
  });
});

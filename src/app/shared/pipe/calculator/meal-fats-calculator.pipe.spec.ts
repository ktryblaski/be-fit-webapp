import { MealFatsCalculatorPipe } from './meal-fats-calculator.pipe';

describe('MealFatsCalculatorPipe', () => {
  it('create an instance', () => {
    const pipe = new MealFatsCalculatorPipe();
    expect(pipe).toBeTruthy();
  });
});

import { MealProteinsCalculatorPipe } from './meal-proteins-calculator.pipe';

describe('MealProteinsCalculatorPipe', () => {
  it('create an instance', () => {
    const pipe = new MealProteinsCalculatorPipe();
    expect(pipe).toBeTruthy();
  });
});

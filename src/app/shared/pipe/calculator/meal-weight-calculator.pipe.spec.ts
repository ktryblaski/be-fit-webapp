import { MealWeightCalculatorPipe } from './meal-weight-calculator.pipe';

describe('MealWeightCalculatorPipe', () => {
  it('create an instance', () => {
    const pipe = new MealWeightCalculatorPipe();
    expect(pipe).toBeTruthy();
  });
});

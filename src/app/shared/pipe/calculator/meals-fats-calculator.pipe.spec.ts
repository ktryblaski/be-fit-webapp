import { MealsFatsCalculatorPipe } from './meals-fats-calculator.pipe';

describe('MealsFatsCalculatorPipe', () => {
  it('create an instance', () => {
    const pipe = new MealsFatsCalculatorPipe();
    expect(pipe).toBeTruthy();
  });
});

import { MealsProteinsCalculatorPipe } from './meals-proteins-calculator.pipe';

describe('MealsProteinsCalculatorPipe', () => {
  it('create an instance', () => {
    const pipe = new MealsProteinsCalculatorPipe();
    expect(pipe).toBeTruthy();
  });
});

import { MealsWeightCalculatorPipe } from './meals-weight-calculator.pipe';

describe('MealsWeightCalculatorPipe', () => {
  it('create an instance', () => {
    const pipe = new MealsWeightCalculatorPipe();
    expect(pipe).toBeTruthy();
  });
});
